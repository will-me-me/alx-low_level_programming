#include "main.h"

/**
 * mul - Getting The Product two integers
 * @a: first integer
 * @b: second integer
 *
 * Return: The product of a and b
 */
int mul(int a, int b)
{
	int product = (a * b);

	return (product);
}
